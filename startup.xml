<?xml version="1.0" encoding="UTF-8"?>
<mod>
    <action name="startup">
        <code>
def startup():
    human = action('make-human', {})
    human.alive = True
    
    map0 = action('newmap', {'mapType':'test'})
    map0.setAlive()
    
    spawn = map0.attr('centre')
    map0.putOn(spawn[0], spawn[1], 'monster', human)
    
    return human

_return = startup()
        </code>
    </action>
</mod>
