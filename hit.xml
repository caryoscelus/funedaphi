<?xml version="1.0" encoding="UTF-8"?>
<mod>
    <action name="hit">
        <object name="subject"/>
        <object name="target"/>
        <number name="amount"/>
        
        <code>
def hit(subject, target, amount):
    target.changeNumericAttr('hp', -amount)
    return True

_return = hit(subject, target, amount)
        </code>
    </action>
</mod>
